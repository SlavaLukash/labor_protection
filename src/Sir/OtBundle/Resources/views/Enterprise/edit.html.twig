{% extends '::base.html.twig' %}

{% block submenu %}
    {{ include('SirOtBundle:Submenu:Administration.html.twig') }}
{% endblock %}

{% block body -%}
<div class="b-form-area">
    <div class="b-form-title"><p>{% trans %}Enterprise edit{% endtrans %}</p><hr/></div>
    {{ form(edit_form) }}
    <hr/>

    {{ form(edit_form) }}

        <ul class="record_actions_in_form">
            <li>
                <a href="{{ path('enterprise') }}">
                    {% trans %}Back to the list{% endtrans %}
                </a>
            </li>
            <li>{{ form(delete_form) }}</li>
        </ul>
</div>
{% endblock %}
