{% extends '::base.html.twig' %}

{% block submenu %}
    {{ include('SirOtBundle:Submenu:Administration.html.twig') }}
{% endblock %}

{% block body -%}
<div class="b-form-area">
    <div class="b-form-title"><p>Enterprise edit</p><hr/></div>
    {{ form(edit_form) }}
    <hr/>

    {{ form(edit_form) }}

        <ul class="record_actions">
            <li>
                <a href="{{ path('enterprise') }}">
                    Back to the list
                </a>
            </li>
            <li>{{ form(delete_form) }}</li>
        </ul>
</div>
{% endblock %}
