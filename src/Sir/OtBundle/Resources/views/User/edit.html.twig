{% extends '::base.html.twig' %}

{% block body -%}
<div class="b-form-area">
    <div class="b-form-title"><p>User edit</p><hr/></div>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            {% trans %}Back to the list{% endtrans %}
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
    </div>
{% endblock %}
