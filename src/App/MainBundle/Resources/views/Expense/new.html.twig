{% extends '::base.html.twig' %}

{% block submenu %}
    {{ include('MainBundle:Submenu:Expense.html.twig') }}
{% endblock %}

{% block body -%}
<div class="b-form-area">
	<div class="b-form-title"><p>{% trans %}Expense creation{% endtrans %}</p><hr/></div>

    {{ form(form) }}

        <ul class="record_actions_in_form">
    <li>
        <a href="{{ path('expense') }}">
			{% trans %}Back to the list{% endtrans %}
        </a>
    </li>
</ul>
	</div>
{% endblock %}
