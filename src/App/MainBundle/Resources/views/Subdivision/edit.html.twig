{% extends 'MainBundle:CRUD:edit.html.twig' %}

{% block submenu %}
    {{ include('MainBundle:Submenu:Administration.html.twig') }}
{% endblock %}

{% block breadcrumbs %}
    {% if isNew %}
        {{ app.breadcrumbs.prepend('Добавление пользователя') }}
    {% else %}
        {{ app.breadcrumbs.prepend('User edit'|trans) }}
    {% endif %}
    {{ app.breadcrumbs.prepend('Пользователи', path('user_list')) }}
    {{ parent() }}
{% endblock %}

{% block panel_title %}
    {% if isNew %}
        {% trans %}Subdivision creation{% endtrans %}
    {% else %}
        {% trans %}Subdivision edit{% endtrans %}
    {% endif %}
{% endblock %}

{% block actions %}
    {% include 'MainBundle:CRUD:form_actions.html.twig' with {
        'is_new': isNew,
        'route_list': path('subdivision_list'),
        'route_remove' : not isNew ? path('subdivision_remove', {'id': entity.id}) : ''
    } %}
{% endblock %}