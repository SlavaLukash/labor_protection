{% extends 'MainBundle:CRUD:edit.html.twig' %}

{% block submenu %}
    {{ include('MainBundle:Submenu:Administration.html.twig') }}
{% endblock %}

{% block breadcrumbs %}
    {% if isNew %}
        {{ app.breadcrumbs.prepend('Добавление пользователя') }}
    {% else %}
        {{ app.breadcrumbs.prepend('User edit'|trans) }}
    {% endif %}
    {{ app.breadcrumbs.prepend('Пользователи', path('user_list')) }}
    {{ parent() }}
{% endblock %}

{% block panel_title %}
    {% if isNew %}
        {% trans %}Profession creation{% endtrans %}
    {% else %}
        {% trans %}Profession edit{% endtrans %}
    {% endif %}
{% endblock %}

{% block actions %}
    {% include 'MainBundle:CRUD:form_actions.html.twig' with {
        'is_new': isNew,
        'route_list': path('profession_list'),
        'route_remove' : not isNew ? path('profession_remove', {'id': entity.id}) : ''
    } %}
{% endblock %}