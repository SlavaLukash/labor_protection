{% extends 'MainBundle:CRUD:edit.html.twig' %}

{% block submenu %}
    {{ include('MainBundle:Submenu:Administration.html.twig') }}
{% endblock %}

{% block breadcrumbs %}
    {% if isNew %}
        {{ app.breadcrumbs.prepend('Добавление пользователя') }}
    {% else %}
        {{ app.breadcrumbs.prepend('User edit'|trans) }}
    {% endif %}
    {{ app.breadcrumbs.prepend('Пользователи', path('user_list')) }}
    {{ parent() }}
{% endblock %}

{% block panel_title %}
    {% if isNew %}
        {% trans %}Traumacause creation{% endtrans %}
    {% else %}
        {% trans %}Traumacause edit{% endtrans %}
    {% endif %}
{% endblock %}

{% block actions %}
    {% include 'MainBundle:CRUD:form_actions.html.twig' with {
        'is_new': isNew,
        'route_list': path('traumacause_list'),
        'route_remove' : not isNew ? path('traumacause_remove', {'id': entity.id}) : ''
    } %}
{% endblock %}