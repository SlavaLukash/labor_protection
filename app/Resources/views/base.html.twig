<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Управление Труда и Социальной Защиты Населения НАО{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('bundles/sirot/css/reset.css') }}" type="text/css" media="all" />
            <link rel="stylesheet" href="{{ asset('bundles/sirot/css/main.css') }}" type="text/css" media="all" />
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('bundles/sirot/js/jquery-1.10.2.min.js') }}"></script>
            <script src="{{ asset('bundles/sirot/js/common.js') }}"></script>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="b-line-top"></div>
        <div id="b-wrapper">
            <div id="b-header">
                <div class="b-line-wrap">
                    <div class="l-line-wrap-l"><div class="bd-line"></div></div>
                    <div class="b-logo"><a href="{{ path('sir_ot') }}"><img src="/bundles/sirot/images/logo.png" /></a></div>
                    <div class="l-line-wrap-r"><div class="bd-line"></div></div>
                </div>
                <div class="clear"></div>
                <h1 class="l-title">Управление Труда и Социальной Защиты Населения НАО</h1>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    {{ include('SirOtBundle:Submenu:MainMenu.html.twig') }}
                {% endif %}
            </div>
            <div class="clear"></div>
            <div id="b-content-werapper">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    {% block submenu %}{% endblock %}
                {% endif %}

                <div id="b-content">
                    {% for type, messages in app.session.flashbag.all() %}
                        {% for key, message in messages %}
                            <div class="flash-{{ type }}">
                                {{ message|trans({}, 'FOSUserBundle') }}
                            </div>
                        {% endfor %}
                    {% endfor %}

                    {% block body %}{% endblock %}
                </div>
            </div>
            <div class="clear"></div>
            <div id="b-footer">
                <div class="b-footer-left">
                    <a href="#">Информация о программе</a>
                </div>
                <div class="b-footer-right">
                    <p>&copy;Copyright, 2013</p>
                </div>
            </div>
        </div>
    </body>
</html>
